<%= form_for(@patient_payment) do |f| %>
  <% if @patient_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@patient_payment.errors.count, "error") %> prohibited this patient_payment from being saved:</h2>

      <ul>
      <% @patient_payment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br />
    <%= f.number_field :amount %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.datetime_select :date %>
  </div>
  <div class="field">
    <%= f.label :patient_charge_id %><br />
    <%= f.number_field :patient_charge_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
