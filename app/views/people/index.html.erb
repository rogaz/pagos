<h2>Personas</h2>

<table class="table table-striped table-bordered">
  <tr>
    <th>Nombre</th>
    <th>Categoria</th>
    <th>Costo</th>
    <th>CENAAC</th>
    <th>Costo</th>
    <!--<th></th>
    <th></th>
    <th></th>-->
  </tr>

<% @people.each do |person| %>
  <tr class="ClickableRow" href="<%= people_charges_path(person) %> ">
    <td><%= person.name %></td>
    <td><%= person.student.nil? ? "" : person.student.category.name %></td>
    <td><%= person.student.nil? ? "" : "$#{person.student.cost}.00" %></td>
    <td><% unless  person.patient.nil? %><i class="icon-ok"></i><% end %></td>
    <td><%= person.patient.nil? ? "" : "$#{person.patient.cost}.00" %></td>
    <!--<td><%= link_to 'Ver Cargos', people_charges_path(person) %></td>
    <td><%= link_to 'Detalles', person %></td>
    <td><%= link_to 'Editar', edit_person_path(person) %></td>
    <td><%= link_to 'Eliminar', person, method: :delete, data: { confirm: "¿Esta seguro que quiere eliminar a #{person.name}?" } %></td>-->

  </tr>
<% end %>
</table>

<br />

<%= link_to 'Registrar Persona', new_person_path, :class => "btn btn-info" %><br/>
